import {
  addProject,
  addTask,
  delProject,
  hideModal,
  showProjectModal,
  showTaskModel,
} from './DOM-stuff'

// Stores the project elements
export const allProjects = []

showProjectModal()
showTaskModel()
hideModal()
addProject()
delProject()
addTask()
